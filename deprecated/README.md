# Deprecated Scripts

These scripts are retained for reference only and are NOT part of the current
AMI build process. They were used in the old ZeroTier/on-prem bursting workflow.

- `05_run_via_ssm.sh` — Was used to run Deadline commands on workers via AWS SSM
  Session Manager. No longer needed; Portal workers connect via the Portal Gateway.
